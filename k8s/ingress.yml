apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: banao-ingress
  labels:
    app.kubernetes.io/name: banao-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: sandbox-service
            port: 
              number: 80

  - host: "*.preview.localhost"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: router-service
            port:
              number: 80
